# DevSpace: React Blog & Tools Suite

Welcome to **DevSpace**, a modern Static Site Generation (SSG) style blog and developer utility application. This project is built with React 18, TypeScript, and Tailwind CSS.

## 🚀 Features

### 1. The Blog
- **Markdown Rendering**: Blog posts are written in Markdown and rendered safely using `react-markdown`.
- **Filtering**: Real-time search by title or tags.
- **Responsive Typography**: Beautifully typeset articles using `@tailwindcss/typography`.

### 2. The Tools
- **JSON Formatter**:
  - Validation: Instantly checks for JSON syntax errors.
  - Prettify: Formats minified JSON into a readable structure.
  - Minify: Compresses JSON for production use.
- **Gradient Generator**:
  - Visual Editor: Create linear CSS gradients.
  - Randomizer: Get inspired with random color combinations.
  - Copy-Paste: One-click CSS export.

## 🛠 Tech Stack

- **Core**: React 18, TypeScript
- **Styling**: Tailwind CSS (via CDN with Typography plugin)
- **Routing**: React Router DOM (HashRouter)
- **Icons**: Lucide React
- **Content**: Static Markdown (simulated file system)

## 🏁 Getting Started

Since this project follows a specific Single Page Application (SPA) structure provided by the environment, no complex build step is required for the initial view if served correctly.

### Prerequisites

- Node.js (v16+)
- npm or yarn

### Installation & Running

1. **Install Dependencies**:
   ```bash
   npm install react react-dom react-router-dom lucide-react react-markdown
   npm install -D typescript @types/react @types/react-dom
   ```

2. **Run Development Server**:
   If using Vite (Recommended):
   ```bash
   npm create vite@latest .
   npm install
   npm run dev
   ```

   If using Create React App:
   ```bash
   npm start
   ```

### Project Structure

```
/
├── components/         # Reusable UI components
│   ├── tools/          # Specific tool implementations
│   └── Layout.tsx      # Main site layout (Nav/Footer)
├── pages/              # Page components (Home, Blog, Tools)
├── services/           # Data layer (Mock Markdown content)
├── App.tsx             # Routing Logic
├── index.tsx           # Entry Point
└── index.html          # HTML Shell
```

## 📝 Adding New Posts

Navigate to `services/content.ts`. Add a new object to the `blogPosts` array:

```typescript
{
  slug: 'my-new-post',
  title: 'My New Post Title',
  date: '2023-12-25',
  author: 'You',
  tags: ['New', 'Topic'],
  excerpt: 'A short summary...',
  content: `
# Markdown Header
Your content here...
  `
}
```

## 🎨 Customizing Styles

Tailwind classes are used throughout. The configuration in `index.html` extends the theme to include specific brand colors (`primary`, `secondary`, `dark`).

---
*Generated by Gemini*
